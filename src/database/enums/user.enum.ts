export enum UserRole {
  ANONYMOUS = "anonymous",
  CUSTOMER = "customer",
  PROFESSIONAL = "professional",
  ADMIN = "admin"
}

export enum ProfessionalVisibility {
  PUBLIC = "public",
  PRIVATE = "private"
}